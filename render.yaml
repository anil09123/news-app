services:
  - type: web
    name: newspulse
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEWS_API_KEY
        sync: false
